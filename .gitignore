# .NET
bin/
obj/
*.user
*.suo
.vs/
*.nupkg

# EF Core migrations SQLite (not used, but just in case)
*.db
*.db-shm
*.db-wal

# Node
node_modules/
dist/
.vite/

# Environment / secrets
.env

# Local-only Docker Compose overrides
docker-compose.override.yml

# IDE
.idea/
.vscode/settings.json

# OS
.DS_Store
Thumbs.db

# Local runtime backup files (created by the app, not source)
/src/Gantry.Api/backups/
